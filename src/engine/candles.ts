import { Candle, Transaction } from '../database/sql_models';
import { SQLDatabase } from '../database/sql_database';



const sql_database = new SQLDatabase();

export class CandleEngine {
    

    public add_transaction() {}
}